# Color Generator
A palette generating app, it uses the thecolorapi to generate unique palettes.

## Features
- Uses css grid for layout
- A dynamically generated array acting as source of truth
- Async and await to synchronize render functions with driver function
- Clipboard API

## Built With
HTML, JSS, CSS